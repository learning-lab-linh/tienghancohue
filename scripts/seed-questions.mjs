/**
 * Trước đây import dữ liệu từ JSON vào SQLite.
 * Giờ ứng dụng chỉ dùng file JSON (json/Listen.json, json/Reading.json, json/quiz-sets.json).
 * Không cần chạy seed nữa.
 */
console.log(
  "Không cần seed: câu hỏi và đề nằm trong json/Listen.json, json/Reading.json và json/quiz-sets.json."
);
